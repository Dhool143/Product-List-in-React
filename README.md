# React + Vite

# React Product List

This project displays a static list of products using React.

## Features
- Uses React components
- Uses JavaScript map() to render product list
- Displays product name, description, and price

## Run the project

npm install
npm run dev
